# 锁定官方 registry。本机 ~/.npmrc 指向 npmmirror 镜像，bun install 会把镜像的
# **绝对 tarball URL** 写进 bun.lock，而 Cloudflare Workers Builds 会照这些 URL 拉包——
# 镜像还没同步某个新版本时，构建直接 404 失败
# （2026-09-17：@aws-sdk/middleware-flexible-checksums@3.974.55 就是这样挂的）。
# 项目级锁定官方源后，lockfile 里的 URL 在任何机器上都可解析。
registry=https://registry.npmjs.com/
