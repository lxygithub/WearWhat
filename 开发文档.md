# 今天穿什么 · 产品方案

> 项目名：今天穿什么  
> 英文名：WearWhat  
> Slogan：别问，问就是它。  
> 副标语：你负责出门，它负责搭。  
> 品牌气质：每天替你纠结三分钟。  
> 目标平台：Android / iOS / PC Web  
> 技术栈：Flutter + 本地 AI + 可选云端 API + 自建后端（PostgreSQL）  
> 版本：v1.2  
> 日期：2026-09-16

---

## 1. 项目定位

**今天穿什么（WearWhat）** 是一款帮你管理四季衣物、自动搭配、解决“今天穿什么”这个每日难题的 App。

核心价值：

- 把衣柜装进手机：拍照录入，自动识别分类。
- 按季节、天气、场合推荐穿搭。
- 记住你穿过什么，避免重复，也避免闲置。
- 换季自动提醒，收纳不再靠脑子。
- 支持自然语言搜索：“那件蓝色条纹衬衫在哪？”
- 默认本地运行，数据在自己手里；可选云端同步与增强。
- 后端主数据库统一使用 **PostgreSQL + pgvector**。

一句话总结：

> 别问，问就是它。

---

## 2. 核心功能

### 2.1 衣物录入

- **拍照录入**：拍一张衣服照片，AI 自动识别类别（上衣 / 裤子 / 裙子 / 外套 / 鞋 / 包 / 配饰）。
- **标签识别**：拍照识别衣物标签上的材质、尺码、洗涤说明。
- **手动补充**：颜色、季节、场合、品牌、购买日期、价格。
- **批量导入**：从相册多选，一次性录入多件。
- **快速录入**：只拍一张，其他交给 AI，后续可再编辑。

### 2.2 衣橱管理

- **分类视图**：按类别、季节、颜色、材质、场合筛选。
- **季节标签**：春 / 夏 / 秋 / 冬 / 四季通用。
- **收纳状态**：在穿 / 已收纳 / 待清洗 / 待修补 / 已淘汰。
- **穿着次数**：自动记录每件衣服穿了多少次。
- **闲置提醒**：超过 N 天没穿，提醒你处理或重新搭配。
- **衣橱统计**：总件数、总价值、利用率、最常穿的颜色。

### 2.3 AI 搭配推荐

- **每日推荐**：根据天气、温度、场合，推荐 3 套搭配。
- **一键换装**：对推荐结果不满意，可单独替换上衣 / 裤子 / 鞋。
- **场合模式**：通勤 / 休闲 / 运动 / 约会 / 正式 / 居家。
- **风格偏好**：用户可设定喜欢的风格，AI 逐渐学习。
- **避雷设置**：不穿短裤、不穿高跟鞋、不穿某颜色等。
- **历史去重**：最近穿过的搭配，短期内不重复推荐。

### 2.4 穿着记录与日历

- **每日记录**：今天穿了哪套，一键记录。
- **穿搭日历**：按月查看每天穿了什么。
- **照片日记**：可选拍一张当日穿搭，留作回忆。
- **统计回顾**：本月最常穿的单品、最闲置的单品。

### 2.5 换季与收纳

- **换季提醒**：气温变化时，提醒你该收什么、该拿什么。
- **收纳清单**：生成“该收起来”和“该拿出来”的清单。
- **收纳位置**：记录每件衣服收在哪个箱子 / 哪个柜子。
- **季节切换**：一键切换当前季节，衣橱只显示当季衣物。

### 2.6 搜索与问答

- **自然语言搜索**：“那件蓝色条纹衬衫”“上次面试穿的那套”。
- **语义检索**：不用精确关键词，描述个大概就能找到。
- **图片搜索**：上传一张网图，找相似单品。
- **问答式查询**：“我有几件白衬衫？”“哪件外套最久没穿？”

### 2.7 愿望清单与购物建议

- **愿望清单**：想买但还没买的衣服，先记下来。
- **搭配缺口分析**：根据现有衣橱，建议“缺一件百搭外套”。
- **重复购买提醒**：想买的白衬衫，你已经有三件了。
- **预算跟踪**：月度 / 年度购衣支出统计。

---

## 3. AI 搭配逻辑概览

> 详细设计见附录 E。

### 3.1 本地 AI 能力

- **图像识别**：识别衣物类别、颜色、图案、材质。
- **标签 OCR**：识别洗标上的文字信息。
- **向量检索**：把每件衣服编码成向量，支持语义搜索。
- **规则引擎**：基于温度、场合、季节的硬规则搭配。

### 3.2 云端增强（可选）

- **LLM 搭配建议**：本地算力不足时，调用云端大模型生成搭配方案。
- **天气 API**：获取实时天气与未来预报。
- **风格学习**：云端模型根据用户历史偏好，生成更个性化的建议。

### 3.3 推荐流程

1. 获取今日天气（温度、降水、风力）。
2. 读取用户场合（通勤 / 休闲 / 运动等）。
3. 从当季衣橱中筛选候选单品。
4. 本地规则引擎生成基础搭配。
5. 可选：调用云端 LLM 优化搭配与文案。
6. 返回 3 套推荐，用户可微调或换一批。
7. 用户选择后，记录到穿搭日历。

### 3.4 本地 / 云端切换

| 模式 | 行为 |
|---|---|
| 仅本地 | 完全离线，规则引擎 + 本地小模型 |
| 仅云端 | 搭配建议交给云端 LLM，本地只做录入与存储 |
| 自动 | 优先本地；本地算力不足或任务复杂时，提示切换云端 |

---

## 4. 四季衣物管理

### 4.1 季节分类

每件衣服可打多个季节标签：

- 春：薄外套、长袖衬衫、针织开衫
- 夏：短袖、短裤、裙子、凉鞋
- 秋：卫衣、风衣、薄毛衣、牛仔裤
- 冬：羽绒服、大衣、厚毛衣、靴子
- 四季通用：内衣、袜子、部分配饰

### 4.2 换季逻辑

- 当气温连续 N 天低于 / 高于阈值，触发换季提醒。
- 生成“该收起来”和“该拿出来”清单。
- 用户确认后，批量更新衣物状态为“已收纳”或“在穿”。
- 收纳位置可记录，方便来年找。

### 4.3 季节视图

- 默认只显示当季衣物。
- 可手动切换到其他季节查看。
- 支持“四季全览”模式，查看全部衣橱。

---

## 5. 技术架构

### 5.1 整体架构

```text
┌──────────────────────────────────────────────┐
│         Flutter UI（Android / iOS / Web）      │
├──────────────────────────────────────────────┤
│      业务逻辑层（纯 Dart，跨端共享）            │
│  衣物 CRUD / 搭配引擎 / 日历 / 搜索 / 同步      │
├──────────────────────────────────────────────┤
│      AI 能力层                                 │
│  图像识别 / OCR / 向量检索 / LLM Provider      │
├──────────────────────────────────────────────┤
│      推理后端（本地优先，云端可选）              │
│  Android/iOS: llama.cpp / TFLite               │
│  Web: WebGPU / Wasm                            │
├──────────────────────────────────────────────┤
│      数据层                                    │
│  本地缓存：SQLite / Drift（离线优先）           │
│  云端主库：PostgreSQL + pgvector + MinIO        │
└──────────────────────────────────────────────┘
```

### 5.2 技术栈选型

| 层级 | 推荐方案 | 说明 |
|---|---|---|
| UI 框架 | Flutter | 三端一套代码 |
| 本地缓存 | Drift（SQLite） | 移动端离线优先，三端统一 ORM |
| 云端主库 | **PostgreSQL 16 + pgvector** | 关系数据 + 向量检索一体 |
| 对象存储 | MinIO / S3 | 衣物照片 |
| 图像识别 | TFLite / ONNX Runtime | 衣物分类、颜色识别 |
| OCR | flutter_paddle_ocr | 洗标文字识别 |
| 向量检索 | 本地 HNSW + 云端 pgvector | 语义搜索 |
| 本地 LLM | flutter_gemma / llamadart | 搭配建议（可选） |
| 云端 API | Dio + Provider 抽象 | OpenAI / Gemini / DeepSeek 等 |
| 后端框架 | FastAPI / NestJS | 自建同步与备份 |
| 天气 | OpenWeather / 和风天气 | 实时天气与预报 |
| 部署 | Docker Compose + Caddy | 一键部署，自动 HTTPS |

---

## 6. 数据模型（PostgreSQL）

> 需要先启用 pgvector 扩展：`CREATE EXTENSION IF NOT EXISTS vector;`

```sql
-- 用户
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- 衣物
CREATE TABLE clothing_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  name TEXT,
  category TEXT NOT NULL,          -- 上衣 / 裤子 / 裙子 / 外套 / 鞋 / 包 / 配饰
  color TEXT,
  pattern TEXT,                    -- 纯色 / 条纹 / 格子 / 印花
  material TEXT,
  seasons TEXT[] DEFAULT '{}',     -- spring / summer / autumn / winter / all
  occasions TEXT[] DEFAULT '{}',   -- 通勤 / 休闲 / 运动 / 正式
  brand TEXT,
  size TEXT,
  price NUMERIC,
  purchase_date DATE,
  storage_status TEXT DEFAULT 'wearing', -- wearing / stored / laundry / repair / discarded
  storage_location TEXT,
  wear_count INT DEFAULT 0,
  last_worn_at TIMESTAMPTZ,
  image_path TEXT,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- 穿搭记录
CREATE TABLE outfits (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  date DATE NOT NULL,
  occasion TEXT,
  weather JSONB,
  notes TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- 穿搭与衣物的关联
CREATE TABLE outfit_items (
  outfit_id UUID REFERENCES outfits(id) ON DELETE CASCADE,
  clothing_item_id UUID REFERENCES clothing_items(id) ON DELETE CASCADE,
  PRIMARY KEY (outfit_id, clothing_item_id)
);

-- 向量索引
CREATE TABLE clothing_embeddings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  clothing_item_id UUID NOT NULL REFERENCES clothing_items(id) ON DELETE CASCADE,
  embedding VECTOR(768),
  description TEXT
);

-- 愿望清单
CREATE TABLE wishlist (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  category TEXT,
  expected_price NUMERIC,
  priority INT DEFAULT 0,
  notes TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- 索引
CREATE INDEX idx_clothing_user ON clothing_items(user_id);
CREATE INDEX idx_clothing_category ON clothing_items(category);
CREATE INDEX idx_outfits_user_date ON outfits(user_id, date);
CREATE INDEX idx_embeddings_item ON clothing_embeddings(clothing_item_id);
CREATE INDEX idx_embeddings_vector ON clothing_embeddings USING hnsw (embedding vector_cosine_ops);
```

---

## 7. 本地与云端策略

### 7.1 默认本地

- 衣物数据、照片、穿搭记录默认存本地 SQLite。
- 图像识别与 OCR 本地运行。
- 搭配推荐优先本地规则引擎。
- 无需联网即可使用核心功能。

### 7.2 可选云端

- **同步与备份**：自建后端 + PostgreSQL，多设备共享衣橱数据。
- **云端 AI 增强**：本地算力不足时，调用云端 LLM 生成搭配建议。
- **天气服务**：调用天气 API 获取实时数据。
- **对象存储**：照片上传到自建 MinIO，节省本地空间。
- **向量检索**：云端 pgvector 支持大规模语义搜索。

### 7.3 隐私与安全

- 云端功能需用户显式开启。
- API Key 使用 `flutter_secure_storage` 加密存储。
- 可选端到端加密：服务端只存密文。
- 敏感照片可标记为“仅本地”。

---

## 8. 分阶段实施路线

### Phase 1 — 本地核心（3-4 周）

- Flutter 三端项目初始化。
- 衣物录入、分类、衣橱管理。
- 本地 SQLite 存储。
- 基础图像识别（类别、颜色）。

### Phase 2 — 搭配与日历（2-3 周）

- 本地规则引擎搭配推荐。
- 天气 API 接入。
- 穿搭日历与穿着记录。
- 换季提醒与收纳管理。

### Phase 3 — AI 增强（2-3 周）

- 向量检索与语义搜索。
- OCR 洗标识别。
- 可选云端 LLM 搭配建议。
- 用户偏好学习。

### Phase 4 — 云端与同步（3-4 周）

- 自建后端 FastAPI + PostgreSQL + pgvector。
- 多设备同步与备份。
- 照片对象存储（MinIO）。
- 端到端加密（可选）。

### Phase 5 — 打磨与上线（2 周）

- 性能优化与内存调优。
- 边界场景处理。
- 应用商店素材与文案。
- 内测与反馈迭代。

---

## 9. 品牌文案

**启动页**
> 今天穿什么  
> 正在翻你的衣柜……

**空衣柜**
> 衣柜空空如也。  
> 先扔几件进来，它才有的挑。

**添加衣物**
> 拍一张，它记住。  
> 你记不住没关系。

**推荐结果**
> 就这套。  
> 别换了，再换迟到。

**天气冷**
> 外面 8 度。  
> 你昨天还想穿短裤，冷静。

**换季提醒**
> 该收短袖了。  
> 不然它以为你一年四季都过夏天。

**设置页**
> 本地模式：衣服数据在你家。  
> 云端模式：衣服数据也在你家，只是多把锁。

**推送**
> 明天 12 度，有雨。  
> 别穿白鞋，听劝。

**衣柜爆满**
> 衣服很多，能穿的没有。  
> 它正在努力。

---

## 10. 结论

**今天穿什么（WearWhat）** 以 Flutter 为统一 UI 与业务层，以本地 AI 为图像识别与搭配核心，以自建后端（PostgreSQL + pgvector）为可选同步与备份方案，覆盖 Android、iOS、PC Web 三端。

核心功能围绕四季衣物管理、AI 搭配推荐、穿着记录与换季提醒展开。默认本地运行，保护隐私；可选云端增强，解决算力不足。

整体技术栈成熟可行，建议从本地核心功能开始，逐步扩展到 AI 增强与云端同步。

> 别问，问就是它。

---

## 附录 A：pubspec.yaml 依赖配置

```yaml
name: wear_what
description: 今天穿什么 - 四季衣物管理与 AI 搭配
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

  # UI 与导航
  go_router: ^14.2.0
  flutter_riverpod: ^2.5.1
  google_fonts: ^6.2.1

  # 本地数据库
  drift: ^2.20.0
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.4
  path: ^1.9.0

  # 图像与相机
  image_picker: ^1.1.2
  camera: ^0.11.0+2
  image: ^4.2.0

  # OCR 与图像识别
  flutter_paddle_ocr: ^1.0.0
  tflite_flutter: ^0.11.0

  # 本地 LLM（可选）
  flutter_gemma: ^0.1.0
  llamadart: ^0.1.0

  # 网络与 API
  dio: ^5.7.0
  connectivity_plus: ^6.0.5
  flutter_secure_storage: ^9.2.2

  # 状态与工具
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  collection: ^1.18.0
  intl: ^0.19.0

  # 图表与统计
  fl_chart: ^0.69.0

  # 分享与导出
  share_plus: ^10.0.2
  markdown: ^7.2.2

  # 权限
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.11
  drift_dev: ^2.20.0
  freezed: ^2.5.7
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/
    - assets/images/
```

> 说明：`flutter_paddle_ocr`、`flutter_gemma`、`llamadart` 的版本号请以 pub.dev 最新为准，这里写的是占位版本。

---

## 附录 B：后端 API 设计

### B.1 认证

| 端点 | 方法 | 说明 |
|---|---|---|
| `/auth/register` | POST | 注册 |
| `/auth/login` | POST | 登录，返回 JWT |
| `/auth/refresh` | POST | 刷新 Token |
| `/auth/forgot-password` | POST | 发送重置邮件 |
| `/auth/reset-password` | POST | 重置密码 |

### B.2 衣物管理

| 端点 | 方法 | 说明 |
|---|---|---|
| `/clothing` | GET | 获取衣物列表（支持筛选） |
| `/clothing` | POST | 新增衣物 |
| `/clothing/{id}` | GET | 获取单件详情 |
| `/clothing/{id}` | PUT | 更新衣物 |
| `/clothing/{id}` | DELETE | 删除衣物 |
| `/clothing/{id}/image` | POST | 上传衣物照片 |
| `/clothing/batch` | POST | 批量导入 |

### B.3 穿搭与日历

| 端点 | 方法 | 说明 |
|---|---|---|
| `/outfits` | GET | 获取穿搭记录 |
| `/outfits` | POST | 记录今日穿搭 |
| `/outfits/{id}` | PUT | 更新穿搭 |
| `/outfits/{id}` | DELETE | 删除记录 |
| `/outfits/recommend` | POST | 获取 AI 搭配推荐 |

### B.4 搜索与统计

| 端点 | 方法 | 说明 |
|---|---|---|
| `/search/semantic` | POST | 语义搜索衣物 |
| `/stats/wear` | GET | 穿着统计 |
| `/stats/closet` | GET | 衣橱统计 |

### B.5 同步与备份

| 端点 | 方法 | 说明 |
|---|---|---|
| `/sync/push` | POST | 推送本地变更 |
| `/sync/pull` | POST | 拉取云端变更 |
| `/export/markdown` | GET | 导出衣橱数据为 Markdown |
| `/export/json` | GET | 导出完整数据为 JSON |

### B.6 天气

| 端点 | 方法 | 说明 |
|---|---|---|
| `/weather/today` | GET | 今日天气 |
| `/weather/forecast` | GET | 未来 7 天预报 |

---

## 附录 C：UI 页面结构

### C.1 底部导航（4 个 Tab）

| Tab | 页面 | 说明 |
|---|---|---|
| 首页 | `HomePage` | 今日推荐、天气、快速记录 |
| 衣橱 | `ClosetPage` | 全部衣物、分类筛选、搜索 |
| 日历 | `CalendarPage` | 穿搭日历、历史记录 |
| 我的 | `ProfilePage` | 设置、统计、同步、关于 |

### C.2 页面清单

**首页 `HomePage`**
- 今日天气卡片
- 今日推荐穿搭（3 套）
- 一键换装按钮
- 快速记录“今天穿了这套”
- 换季提醒横幅

**衣橱 `ClosetPage`**
- 搜索框（支持自然语言）
- 分类 Tab：全部 / 上衣 / 裤子 / 裙子 / 外套 / 鞋 / 包 / 配饰
- 季节筛选：春 / 夏 / 秋 / 冬 / 四季
- 衣物网格视图
- 点击进入详情

**衣物详情 `ClothingDetailPage`**
- 大图展示
- 类别、颜色、材质、季节、场合
- 穿着次数、最后穿着日期
- 收纳状态与位置
- 编辑 / 删除 / 标记为已收纳

**添加衣物 `AddClothingPage`**
- 拍照 / 从相册选择
- AI 自动识别结果（可编辑）
- 手动补充标签
- 保存

**搭配推荐 `OutfitRecommendPage`**
- 今日天气
- 场合选择
- 3 套推荐搭配
- 单件替换
- 确认穿着

**日历 `CalendarPage`**
- 月视图
- 每天显示穿搭缩略图
- 点击查看当天详情

**统计 `StatsPage`**
- 衣橱总件数、总价值
- 最常穿的单品
- 最闲置的单品
- 颜色分布饼图
- 月度穿着次数折线图

**设置 `SettingsPage`**
- 本地 / 云端模式切换
- 天气城市设置
- 风格偏好
- 避雷设置
- 同步与备份
- 关于

### C.3 路由结构（go_router）

```dart
GoRouter(
  routes: [
    ShellRoute(
      builder: (context, state, child) => MainScaffold(child: child),
      routes: [
        GoRoute(path: '/', builder: (_, __) => HomePage()),
        GoRoute(path: '/closet', builder: (_, __) => ClosetPage()),
        GoRoute(path: '/calendar', builder: (_, __) => CalendarPage()),
        GoRoute(path: '/profile', builder: (_, __) => ProfilePage()),
      ],
    ),
    GoRoute(path: '/clothing/:id', builder: (_, state) => ClothingDetailPage(id: state.pathParameters['id']!)),
    GoRoute(path: '/clothing/add', builder: (_, __) => AddClothingPage()),
    GoRoute(path: '/outfit/recommend', builder: (_, __) => OutfitRecommendPage()),
    GoRoute(path: '/stats', builder: (_, __) => StatsPage()),
    GoRoute(path: '/settings', builder: (_, __) => SettingsPage()),
  ],
)
```

---

## 附录 D：本地缓存表（Drift 示例）

移动端使用 SQLite/Drift 做离线缓存，通过同步引擎与云端 PostgreSQL 保持一致。

```dart
class ClothingItems extends Table {
  TextColumn get id => text()();
  TextColumn get name => text().nullable()();
  TextColumn get category => text()();
  TextColumn get color => text().nullable()();
  TextColumn get pattern => text().nullable()();
  TextColumn get material => text().nullable()();
  TextColumn get seasons => text().withDefault(const Constant(''))();
  TextColumn get occasions => text().withDefault(const Constant(''))();
  TextColumn get brand => text().nullable()();
  TextColumn get size => text().nullable()();
  RealColumn get price => real().nullable()();
  DateTimeColumn get purchaseDate => dateTime().nullable()();
  TextColumn get storageStatus => text().withDefault(const Constant('wearing'))();
  TextColumn get storageLocation => text().nullable()();
  IntColumn get wearCount => integer().withDefault(const Constant(0))();
  DateTimeColumn get lastWornAt => dateTime().nullable()();
  TextColumn get imagePath => text().nullable()();
  DateTimeColumn get createdAt => dateTime().withDefault(currentDateAndTime)();
  DateTimeColumn get updatedAt => dateTime().withDefault(currentDateAndTime)();

  @override
  Set<Column> get primaryKey => {id};
}
```

> 同步策略：本地写入后加入同步队列，联网时通过 `/sync/push` 推送变更到云端 PostgreSQL，再用 `/sync/pull` 拉取其他设备的变更。冲突解决采用 Last-Write-Wins，复杂场景可引入 CRDT。

---

## 附录 E：AI 搭配引擎详细设计

### E.1 设计目标

- 每天给出 **3 套可穿搭配**，解决“今天穿什么”。
- 综合考虑天气、场合、季节、用户偏好。
- 本地优先，云端可选增强。
- 结果可解释、可调整、可学习。
- 避免重复穿着，提升衣橱利用率。

### E.2 输入与输出

**输入**

| 类型 | 内容 |
|---|---|
| 天气 | 温度、体感温度、降水、风力、湿度 |
| 场合 | 通勤 / 休闲 / 运动 / 约会 / 正式 / 居家 |
| 日期 | 日期、星期、当前季节 |
| 用户偏好 | 风格标签、避雷项、颜色偏好 |
| 衣橱 | 当季可用衣物（状态为 wearing） |
| 历史 | 最近穿着记录、每件衣服穿着次数 |

**输出**

- 3 套搭配方案，每套包含：
  - 单品列表（上衣、裤子/裙子、鞋、外套等）
  - 综合评分
  - 推荐理由
  - 风格标签
- 可替换单品建议
- 可选：云端 LLM 生成的文案解释

### E.3 整体流程

1. 获取今日天气与用户场合。
2. 硬过滤：季节、收纳状态、避雷项、温度硬边界。
3. 候选单品打分。
4. 按类别生成组合。
5. 组合评分。
6. 去重与多样性筛选。
7. 本地规则排序。
8. 可选：调用云端 LLM 优化搭配与解释。
9. 返回 Top 3。

### E.4 本地规则引擎

#### 硬规则（过滤）

直接排除不符合条件的单品：

- 季节不匹配：如冬天不推荐短袖。
- 收纳状态不是 `wearing`。
- 命中用户避雷项：不穿短裤、不穿高跟鞋、不穿某颜色。
- 温度硬边界：低于 5°C 不推荐短袖；高于 30°C 不推荐羽绒服。
- 场合硬约束：正式场合不推荐运动裤、拖鞋。

#### 软规则（打分）

对通过硬过滤的单品和组合进行评分：

| 因子 | 说明 |
|---|---|
| 温度适宜度 | 根据材质、厚度估算保暖等级，与天气匹配 |
| 场合匹配度 | 单品标签与场合的匹配程度 |
| 颜色和谐度 | 同色系、互补色、中性色搭配规则 |
| 用户偏好 | 风格标签与用户偏好的匹配度 |
| 新鲜度 | 最近穿过的单品降权 |
| 利用率 | 太久没穿的轻微加权，促进利用 |
| 重复惩罚 | 与最近穿搭高度重复的组合降权 |

### E.5 打分算法

**单品评分**

```text
score_item =
  w1 * temp_fit
  + w2 * occasion_fit
  + w3 * color_harmony
  + w4 * user_pref
  + w5 * freshness
  + w6 * utilization
```

**组合评分**

```text
score_outfit =
  avg(item_scores)
  + w7 * color_cohesion
  + w8 * style_cohesion
  + w9 * weather_adaptation
  - w10 * repetition_penalty
```

默认权重建议：

| 权重 | 默认值 | 说明 |
|---|---|---|
| w1 | 0.30 | 温度适宜度最重要 |
| w2 | 0.25 | 场合匹配 |
| w3 | 0.15 | 颜色和谐 |
| w4 | 0.10 | 用户偏好 |
| w5 | 0.10 | 新鲜度 |
| w6 | 0.05 | 利用率 |
| w7 | 0.15 | 组合颜色协调 |
| w8 | 0.10 | 组合风格统一 |
| w9 | 0.15 | 整体天气适应 |
| w10 | 0.20 | 重复惩罚 |

权重可配置，也可通过用户反馈学习调整。

### E.6 伪代码

```dart
List<Outfit> recommendOutfits({
  required Weather weather,
  required Occasion occasion,
  required List<ClothingItem> closet,
  required UserPreference pref,
  required List<Outfit> recentOutfits,
  int count = 3,
}) {
  // 1. 硬过滤
  final candidates = closet.where((item) {
    if (item.storageStatus != 'wearing') return false;
    if (!item.seasons.contains(weather.season) &&
        !item.seasons.contains('all')) return false;
    if (pref.avoidCategories.contains(item.category)) return false;
    if (weather.temp < 5 && item.category == '短袖') return false;
    if (weather.temp > 30 && item.category == '羽绒服') return false;
    if (occasion == Occasion.formal && item.category == '运动裤') return false;
    return true;
  }).toList();

  // 2. 按类别分组
  final tops = candidates.where((i) => i.category == '上衣').toList();
  final bottoms = candidates
      .where((i) => i.category == '裤子' || i.category == '裙子')
      .toList();
  final shoes = candidates.where((i) => i.category == '鞋').toList();
  final outer = candidates.where((i) => i.category == '外套').toList();

  // 3. 生成组合
  final combos = <Outfit>[];
  for (final top in tops) {
    for (final bottom in bottoms) {
      for (final shoe in shoes) {
        if (weather.temp < 15 && outer.isNotEmpty) {
          for (final o in outer) {
            combos.add(Outfit(items: [top, bottom, shoe, o]));
          }
        } else {
          combos.add(Outfit(items: [top, bottom, shoe]));
        }
      }
    }
  }

  // 4. 评分
  for (final combo in combos) {
    combo.score = _scoreOutfit(
      combo,
      weather,
      occasion,
      pref,
      recentOutfits,
    );
  }

  // 5. 排序、去重、多样性
  combos.sort((a, b) => b.score.compareTo(a.score));
  final selected = _diversify(combos, count);

  return selected;
}
```

### E.7 云端 LLM 增强

当本地算力不足，或用户主动选择“云端增强”时，调用云端 LLM 优化搭配。

**提示词模板**

```text
你是一个穿搭助手。根据以下信息推荐 3 套搭配。

天气：{weather}
场合：{occasion}
用户偏好：{pref}
可选单品：{items}
最近穿着：{recent}

请返回 JSON 数组，每套包含：
- item_ids: 单品 ID 列表
- reason: 推荐理由
- style_tags: 风格标签
```

**输出 JSON Schema**

```json
[
  {
    "item_ids": ["uuid1", "uuid2", "uuid3"],
    "reason": "适合 18°C 通勤，颜色协调，正式度适中。",
    "style_tags": ["简约", "通勤"]
  }
]
```

**降级策略**

- 云端调用超时或失败 → 自动回退到本地规则结果。
- 云端返回格式错误 → 丢弃，使用本地结果。
- 隐私敏感衣物 → 强制本地，不发送云端。

### E.8 用户反馈学习

- 记录用户行为：选择、替换、跳过、收藏。
- 更新单品权重：
  - 被选中的单品：权重 +
  - 被替换掉的单品：权重 -
- 更新风格偏好向量：根据用户选择调整。
- 定期重新计算用户偏好权重。
- 支持手动调整：用户可编辑偏好设置。

### E.9 数据结构示例

**Outfit JSON**

```json
{
  "id": "uuid",
  "date": "2026-09-16",
  "occasion": "通勤",
  "weather": {
    "temp": 18,
    "condition": "多云",
    "wind": "3级"
  },
  "items": [
    { "id": "uuid1", "category": "上衣", "name": "白色衬衫" },
    { "id": "uuid2", "category": "裤子", "name": "深蓝牛仔裤" },
    { "id": "uuid3", "category": "鞋", "name": "白色运动鞋" }
  ],
  "score": 87.5,
  "reason": "18°C 通勤，白色衬衫搭配牛仔裤，清爽且正式度适中。",
  "style_tags": ["简约", "通勤"]
}
```

### E.10 性能优化

- **预筛选**：只加载当季、`wearing` 状态的衣物。
- **向量索引**：本地 HNSW，云端 pgvector，加速相似检索。
- **缓存**：当天推荐结果缓存，避免重复计算。
- **懒加载**：图片缩略图按需加载。
- **云端限流**：设置超时与重试上限。
- **增量学习**：用户反馈异步更新，不阻塞推荐。

---

以上为《今天穿什么 · 产品方案》完整版，保存为 `今天穿什么-产品方案.md` 即可。